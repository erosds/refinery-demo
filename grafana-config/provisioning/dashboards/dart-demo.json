{
  "dashboard": {
    "id": null,
    "title": "DART Demo - Refinery AI Analytics",
    "tags": ["dart", "demo", "ai", "refinery"],
    "timezone": "browser",
    "refresh": "5s",
    "time": {
      "from": "now-1h",
      "to": "now"
    },
    "panels": [
      {
        "id": 1,
        "title": "🎯 BIT-TQ Penetrazione Bitume",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
        "targets": [
          {
            "datasource": "TimescaleDB",
            "rawSql": "SELECT timestamp as time, COALESCE(bit_tq, 45.0) as \"BIT-TQ Attuale\" FROM process_data WHERE $__timeFilter(timestamp) ORDER BY timestamp DESC LIMIT 1000",
            "refId": "Current"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "dmm",
            "min": 35,
            "max": 65
          }
        }
      },
      {
        "id": 2,
        "title": "🤖 AI Decisions Count",
        "type": "stat",
        "gridPos": {"h": 4, "w": 6, "x": 12, "y": 0},
        "targets": [
          {
            "datasource": "TimescaleDB",
            "rawSql": "SELECT COUNT(*) as \"Total Decisions\" FROM ai_decisions",
            "refId": "A"
          }
        ]
      },
      {
        "id": 3,
        "title": "📊 Process Data Count",
        "type": "stat",
        "gridPos": {"h": 4, "w": 6, "x": 18, "y": 0},
        "targets": [
          {
            "datasource": "TimescaleDB",
            "rawSql": "SELECT COUNT(*) as \"Data Points\" FROM process_data",
            "refId": "A"
          }
        ]
      }
    ]
  }
}